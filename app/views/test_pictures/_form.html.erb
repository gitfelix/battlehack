<%= form_for(@test_picture) do |f| %>
  <% if @test_picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_picture.errors.count, "error") %> prohibited this test_picture from being saved:</h2>

      <ul>
      <% @test_picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
